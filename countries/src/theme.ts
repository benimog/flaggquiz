import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    background: {
      default: "#282c34",
      paper: "#282c34",
    },
  },
});

export default theme;
